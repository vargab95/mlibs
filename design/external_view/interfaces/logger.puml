@startuml

package "logger" {
  interface "LoggerInterface" {
    createLogger(level, path, consoleLogEnabled)
    setLogLevel(level)
    setLogFilePath(path)
    enableConsoleLog()
    disableConsoleLog()
    writeLogMessage(level, arguments)
  }
}

@enduml
